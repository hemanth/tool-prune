[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "tool-prune"
version = "0.2.0"
description = "Calibrated tool selection and schema pruning for AI agents using TypeSafe System One and TurboQuant vector search."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
    { name = "Hemanth HM", email = "hemanth.hm@gmail.com" },
]
keywords = ["tool-prune", "tool-selection", "turboquant", "mcp", "agents", "typesafe", "routing", "function-calling", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]

[project.optional-dependencies]
turbovec = ["turbovec>=1.0.0", "numpy"]


[project.urls]
Homepage = "https://github.com/hemanth/tool-prune"
Repository = "https://github.com/hemanth/tool-prune"
